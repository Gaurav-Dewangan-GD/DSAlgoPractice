//find the given elements in an array are power of two or not(using set bit logic i.e if no.of set bits is one then it is power of two)
#include<iostream>

int count_set_bits(int arr[], int size)
{

		while(arr[i] !=0)
		{ 
			count +=arr[i] & 1;
			arr[i] = arr[i] >> 1;

int main()
{
	int arr[5] = {1,2,16,7,9};
	int count = 0;
	for(int i = 0; i < 5; i++)
	{
		

	}


